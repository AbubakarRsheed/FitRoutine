/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://fitroutine.online',
  generateRobotsTxt: true, // This also creates a robots.txt file for you
  generateIndexSitemap: false, // Keeps it simple with just one sitemap file
}